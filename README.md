# matrogers.github.io

Personal homepage: a single page with a photo, social links, and a short intro.
Built with Jekyll and hosted on GitHub Pages.

Content lives in `_config.yml` (name, title, intro blurb, social links);
markup is in `_layouts/resume.html`, styles in `_sass/`.

Local preview:

```
bundle install
bundle exec jekyll serve
```
